# 에셋 번들 가이드 — 팀 기본 세트를 앱에 박아 배포 (§16)

팀이 만든 **기본 에셋 세트**(마스크·컬러 아트·질감 맵 PNG)를 앱 번들에 넣어,
설치 직후 컴포저 카탈로그에 바로 뜨게 하는 절차입니다. 원격 서버 없이 파일이
앱과 함께 배포되고, 컴포저의 "카탈로그에서" 스트립에서 탭 한 번으로 적용됩니다.

이 문서는 팀(디자이너·엔지니어)이 **기본 세트를 추가/갱신**할 때 보는 실무 가이드예요.
개별 사용자가 인앱에서 임포트하는 흐름(사진 라이브러리·붙여넣기)과는 별개입니다.

---

## 큰 그림 — 두 파일이 짝을 이룬다

번들 기본 세트는 **파일 두 곳**을 함께 커밋해야 완성됩니다:

1. **PNG 파일** → `unity/Assets/StreamingAssets/catalog/<kind>/` (앱 번들에 복사됨)
2. **카탈로그 항목(JSON)** → `src/assets/catalogDefault.json`의 `entries[]`
   (앱이 시작 시 읽어 카탈로그에 노출)

항목의 `uri`가 `streaming:catalog/<kind>/<파일명>.png` 형식이면, Unity의
`ImageFileLoader.ResolvePath`가 이를 `Application.streamingAssetsPath` 상대경로로
해석해 **번들 안의 실제 PNG를 렌더**합니다. 즉 uri 스킴이 파일과 항목을 잇는 계약이에요.

```
src/assets/catalogDefault.json 항목의 uri
   "streaming:catalog/mask/blush_natural.png"
                    │
                    ▼  (Unity ImageFileLoader.ResolvePath)
   <streamingAssetsPath>/catalog/mask/blush_natural.png   ← 실제 번들 PNG
```

---

## 단계별 절차

### 1) PNG를 StreamingAssets에 넣기

파일을 `kind`별 폴더에 넣습니다(폴더는 이미 생성돼 있어요):

```
unity/Assets/StreamingAssets/catalog/
├── mask/          ← kind: "mask"        (모양 축 스텐실 — "어디에")
├── colorArt/      ← kind: "colorArt"    (텍스처 축 그림 — "무엇을")
└── textureMap/    ← kind: "textureMap"  (#22 광 지도 — "어떻게 빛나는지")
```

- 파일명은 소문자·영숫자·언더스코어 권장(예: `blush_natural.png`).
- 형식은 PNG. 마스크·광 지도는 알파/그레이 채널 의미가 있으니 팀 스펙을 따르세요.

### 2) `catalogDefault.json`에 항목 추가

`src/assets/catalogDefault.json`의 `entries` 배열에 항목을 넣습니다. 한 항목 형태:

```json
{
  "id": "mask-blush-natural",
  "kind": "mask",
  "region": "blush",
  "name": "자연 블러셔 존",
  "uri": "streaming:catalog/mask/blush_natural.png",
  "thumbnail": "data:image/png;base64,iVBORw0KGgo…",
  "tags": ["기본", "자연"],
  "version": 1
}
```

필드 의미:

| 필드 | 필수 | 설명 |
| --- | --- | --- |
| `id` | ✔ | 매니페스트 내 유일 식별자. 같은 `id`를 다시 쓰면 갱신(뒤가 이김). |
| `kind` | ✔ | `"mask"` / `"colorArt"` / `"textureMap"` 중 하나. |
| `region` | ✔ | `kind`별 유효 부위 — 아래 표 참고(정확히 일치해야 항목이 살아남음). |
| `name` | ✔ | 카탈로그 스트립·픽커에 뜨는 표시 이름. |
| `uri` | ✔ | `streaming:catalog/<kind>/<파일명>.png`. 앱이 이걸로 번들 PNG를 로드·적용. |
| `thumbnail` | 선택 | 픽커 미리보기. **data URI 권장**(없으면 이름 타일로 뜸 — 5) 참고). |
| `tags` | 선택 | 검색·필터용 문자열 배열. 없으면 `[]`. |
| `version` | 선택 | 에셋 리비전(양수, 기본 1). 파일 갱신 시 올림. |
| `note` | 선택 | 팀 메모 한 줄. |

> `kind`/`region`이 표와 어긋나거나 `id`·`name`·`uri`가 비면, 앱의 `normalizeEntry`가
> 그 항목을 **조용히 버립니다**(불량 매니페스트 방어). 다른 항목엔 영향 없어요.

### 3) `kind`별 유효 `region` 값 (정확히)

아래 값은 `src/composer/regions.ts`의 타입 유니온에서 그대로 발췌한 것이고,
`src/assets/assetCatalog.ts`의 `MASK_REGIONS`·`COLOR_ART_REGIONS`·`TEXTURE_MAP_REGIONS`와
컴파일 타임에 정합(드리프트하면 빌드가 깨짐)됩니다.

**`kind: "mask"`** — `regions.ts` `MaskRegion` (부위 "존" 스텐실)

| `region` | 부위 |
| --- | --- |
| `blush` | 블러셔 볼 존 |
| `highlighter` | 하이라이터 광채 존 |
| `contour` | 컨투어/섀딩 존 |
| `eyeshadow` | 아이섀도 밴드(로컬 UV) |

**`kind: "colorArt"`** — `regions.ts` `TextureAction` (부위에 칠할 그림)

| `region` | 부위 |
| --- | --- |
| `brow` | 눈썹 스타일 |
| `eyeliner` | 아이라인 그림 |
| `lip` | 립 그림 |
| `blush` | 볼 그림 |
| `aegyo` | 애교살 그림 |

**`kind: "textureMap"`** — `regions.ts` `TextureMapRegion` (픽셀별 광 지도)

| `region` | 부위 |
| --- | --- |
| `lip` | 립 마감 |
| `eyeshadow` | 아이섀도 마감 |
| `blush` | 블러셔 마감 |

### 4) 재빌드 필요

`StreamingAssets`와 `catalogDefault.json`(JS 번들 시드)은 **풀 빌드 때만** 앱에 반영됩니다.
JS만 리로드하면 새 항목·PNG가 반영되지 않아요.

```bash
npm run ios      # 또는 팀 빌드 파이프라인
```

빌드 후 컴포저를 열면 해당 부위 축의 "카탈로그에서" 스트립에 항목이 뜹니다.

### 5) 썸네일 — data URI 권장

번들 항목의 `uri`(`streaming:…`)는 **RN `<Image>`가 못 읽는** 네이티브 전용 스킴이라,
카탈로그 스트립은 `thumbnail`이 렌더 가능한 스킴(`data:`·`file:`·`http(s):`·`content:`·`asset:`)일
때만 이미지를 그리고, 아니면 **이름만 표시하는 타일**로 폴백합니다(깨진 이미지 방지).

- `thumbnail`에 **data URI**를 넣으면 픽커에서 미리보기 썸네일이 뜹니다(권장).
- 생략하면 이름 타일로 떠도 기능은 정상 — 탭하면 `uri`로 실제 PNG가 적용됩니다.

data URI는 웹 GUI(아래)로 뽑는 게 가장 쉽습니다.

### 6) 웹 GUI로 항목 뽑기 (`에셋-카탈로그-빌더.html`)

브라우저로 `에셋-카탈로그-빌더.html`을 열고 **번들 모드**를 켜면, 팀이 클릭으로 항목을
조립해 `catalogDefault.json`에 붙여넣을 JSON을 만들 수 있어요:

1. 상단 "에셋 등록"에서 **번들 모드** 체크박스를 켠다.
2. 종류(`kind`)·부위(`region`)·이름을 고르고, **번들 파일명**에 `blush_natural.png`처럼 입력
   (썸네일을 드래그&드롭하면 그 파일명이 자동으로 채워짐). → `uri`가
   `streaming:catalog/<kind>/<파일명>`으로 자동 생성된다.
3. 썸네일 이미지를 드롭하면 `thumbnail`에 data URI가 담긴다(픽커 미리보기용).
4. "카탈로그에 추가" → 항목이 그리드에 쌓인다. 여러 개 반복.
5. "매니페스트 JSON 내보내기" → 나온 파일의 `entries`를 `src/assets/catalogDefault.json`의
   `entries`에 붙여넣는다(또는 전체 매니페스트로 교체).

번들 모드를 끄면 기존 방식(임의 uri 자유 입력)으로도 계속 쓸 수 있습니다.

---

## 완성형 예시 (항목 1개)

`src/assets/catalogDefault.json`을 빈 시드에서 항목 1개로 채운 모습:

```json
{
  "schema": "armakeup.assetCatalog",
  "version": 1,
  "entries": [
    {
      "id": "mask-blush-natural",
      "kind": "mask",
      "region": "blush",
      "name": "자연 블러셔 존",
      "uri": "streaming:catalog/mask/blush_natural.png",
      "thumbnail": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==",
      "tags": ["기본", "자연"],
      "version": 1
    }
  ]
}
```

여기 대응하는 PNG는 `unity/Assets/StreamingAssets/catalog/mask/blush_natural.png`에 있어야 합니다.

---

## 원격 카탈로그 (§16 v2 — `http(s)://` uri)

번들 파일(`streaming:…`) 외에 **원격 CDN 에셋**도 지원합니다. 항목의 `uri`가
`https://…/asset.png`이면, 탭해서 적용하는 순간 앱이 그 파일을 한 번 내려받아
캐시(`temporaryCachePath/remoteCatalog/`)한 뒤 기존 로더로 렌더합니다
(`RemoteAssetCache` → `ImageFileLoader.ResolvePath`가 캐시 로컬 경로 반환).
렌더러는 캐시된 로컬 파일만 보므로 로직 변화 없이 동작합니다.

발행 방법 두 가지:

- **URL 임포트** — 카탈로그 임포트 모달의 `https://…/manifest.json` 칸에 원격
  매니페스트 URL을 넣고 **URL** 버튼을 누르면, 앱이 `manifest.json`을 페치해
  붙여넣기 임포트와 똑같이 병합합니다(디자이너가 CDN에 올리면 앱에서 즉시 반영).
- **JSON에 원격 uri** — `catalogDefault.json`이나 붙여넣기 매니페스트의 `uri`에
  `https://…`를 직접 넣어도 됩니다.

원격 규약/주의:

- **https 권장(iOS 우선)** — https는 무설정 동작. 평문 `http://`는 iOS `Info.plist`
  `NSAppTransportSecurity` 예외가 없으면 차단되어 "다운로드 실패" 토스트로 뜹니다.
- **스테일 캐시 주의** — 캐시 키는 uri 문자열입니다. 같은 uri로 CDN 내용만 바꾸면
  옛 파일이 서빙되니, **콘텐츠 주소화(`asset-<hash>.png`)** 또는 uri에
  `?v=<version>`을 붙여 갱신 시 uri가 바뀌게 하세요.
- **Android StreamingAssets** — Android는 `streaming:…`도 APK 내부(File 불가)라 같은
  캐시 계층을 경유해 스트리밍합니다(iOS는 실제 폴더라 직접 읽음, 동작 동일).
- 첫 적용은 다운로드가 끝날 때까지(최대 20초 timeout) 지연될 수 있습니다(진행 UI는 후속).

## 한계 / 주의

- **풀 빌드 필요** — PNG(StreamingAssets)도 시드 JSON도 JS 리로드로는 반영 안 됨.
- **번들 항목은 인앱에서 삭제해도 되살아납니다** — 시드는 코드/커밋으로 관리(`removeEntry`는
  사용자 레이어만 지움). 기본 세트를 바꾸려면 `catalogDefault.json`을 고쳐 다시 커밋하세요.
- `id` 충돌 시 **사용자가 임포트한 항목이 번들 기본을 덮습니다**(사용자 우선 병합 규약).
