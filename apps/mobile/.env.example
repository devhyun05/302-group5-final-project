# Use a LAN IP for local device testing, for example http://192.168.0.10:8000/api.
# Use https://<cloudfront-domain>/api for deployed builds.
EXPO_PUBLIC_API_BASE_URL=

# Set to true only for local development builds when repeated face-analysis testing is needed.
EXPO_PUBLIC_FACE_ANALYSIS_USAGE_LIMIT_BYPASS=false
